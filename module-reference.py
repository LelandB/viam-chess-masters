"""Legacy inline-module placeholder.

The module implementation was removed from the active code path because this
project now uses the external, testable Python SDK client in ``robot_app.py``.
The original experiment remains recoverable from Git history.
"""
